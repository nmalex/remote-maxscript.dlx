Install nodejs to run test client: https://nodejs.org/en/

USAGE:
    node -v                          <-- to check that node is installed and get it's version.
    node maxscript-test-client.js    <-- to run maxscript remote console
